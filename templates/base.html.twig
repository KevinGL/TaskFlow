<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body>
        {% if app.user %}
            {% include "navbar.html.twig" %}
        {% endif %}
        <div class="fixed top-4 right-4 space-y-2 z-50">
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                {% set bg_class = 
                    label == 'success' ? 'bg-green-100 text-green-800' :
                    label == 'error'   ? 'bg-red-100 text-red-800' :
                    label == 'warning' ? 'bg-yellow-100 text-yellow-800' :
                    'bg-blue-100 text-blue-800' %}

                    <div class="px-4 py-2 rounded-lg shadow-md {{ bg_class }} flex items-center justify-between min-w-[250px] max-w-xs">
                        <span>{{ message }}</span>
                        <button type="button" class="ml-4 font-bold" onclick="this.parentElement.remove()">×</button>
                    </div>
                {% endfor %}
            {% endfor %}
        </div>
        {% block body %}{% endblock %}
    </body>
</html>
