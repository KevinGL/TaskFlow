{% extends 'base.html.twig' %}

{% block title %}Bienvenue !{% endblock %}

{% block body %}

<a href={{ url("add_task") }}>Ajouter une tâche</a>

<table>
    <tr>
        <th>id</th>
        <th>Nom</th>
        <th>Admin</th>
        <th>Modifier</th>
        <th>Supprimer</th>
    </tr>
    {% for user in users %}
        <tr>
            <td>{{ user.id }}</td>
            <td>{{ user.username }}</td>
            <td>{{ "ROLE_ADMIN" in user.roles  ? "Oui" : "Non" }}</td>
            <td><a href={{ url("task_edit", { id: user.id }) }}>Modifier</a></td>
            <td><a href={{ url("task_delete", { id: user.id }) }}>Supprimer</a></td>
        </tr>
    {% endfor %}
</table>

{% endblock %}
